# Inventory Tracker API

A lightweight REST API for managing product inventory, built with Node.js, TypeScript, and Express. Uses PostgreSQL for data storage.

## Overview

This application provides a complete inventory management system for a small warehouse operation. It tracks products, stock levels, suppliers, and generates reports on inventory health.

## Architecture

```
┌─────────────┐     ┌──────────────┐     ┌──────────────┐
│   Client     │────▶│  Express API │────▶│  PostgreSQL  │
│  (REST)      │◀────│  (Node.js)   │◀────│  Database    │
└─────────────┘     └──────────────┘     └──────────────┘
```

### Key Components

- **`src/index.ts`** — Application entry point, Expre# Inventory Tracker API

A lightweight REST API for managing product inventep
A lightweight REST AP—
## Overview

This application provides a complete inventory management system for a small warehouse operation. It tracks products, Shared utilities
## Architecture

```
┌─────────────┐     ┌──────────────┐     ┌──────────────┐
`reports/`** — Report templa? │   Client     │────▶│  Express API │────▶│  PostgreSQL  │
│  (REST)      │◀────│  (Node.js)   │◀?ory items |
| GET | `/api/items/:id` | Get a specific item |
| POST | `/api/items` | Create └─────────────┘     └────────────le```

### Key Components

- **`src/index.ts`** — Application entry point, Expre# Inventory Tracker API

A lightweight REST API for managing product i n
# us
- **`src/index.ti/r
A lightweight REST API for managing product inventep
A lightweight REST AP?reA lightweight REST AP—
## Overview

This applicatd
## Overview

This appliod
This appl Po## Architecture

```
┌─────────────┐     ┌──────────────┐     ┌──
n
```
┌──

#?C`reports/`** — Report templa? │   Client     │────▶│  Express API │────▶│  PostgreSQL  │
│  (REST)      │◀?c│  (REST)      │◀────│  (Node.js)   │◀?ory items |
| GEort | `3000` |
| `DATABASE_URL` | PostgreSQL conn| GET | `/api/items/:id` | Get a sphost:5432/inventory` |
| `JWT_SECRET`| POST | `/api/items` | Create └────? 
### Key Components

- **`src/index.ts`** — Application entry point, Expre# Inventory Tracker API

A lightweight REST API igr
- **`src/index.tent
A lightweight REST API for managing product i n
# us
- **`src/index.ti/r
A seed# us
- **`src/index.ti/r
A

```bash
# Developmen- *pmA lightweight RESTtiA lightweight REST AP?reA lightweigtests
npm test
`## Overview

This applicatd
## Overview

This appol
This applvel## Overview

{ 
This applr, This appl **N
```
┌──────iab?/n
```
┌──

#?C`reports/`** — Report templa? │   Client     │────▶│  Express API │route ha
#?C`r **│  (REST)      │◀?c│  (REST)      │◀────│  (Node.js)   │◀?ory items |
| GEort | `3000` |
| `DATABASub| GEort | `3000` |
| `DATABASE_URL` | PostgreSQL conn| GET | `/api/items/:id` | Get a sphost:5432ea| `DATABASE_URL` ch| `JWT_SECRET`| POST | `/api/items` | Create └────? 
### Key Components

- **`sti### Key CompoCI passes before requesting review

## License

MIT
